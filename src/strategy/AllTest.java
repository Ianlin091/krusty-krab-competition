package strategy;

public class AllTest {
	public static boolean checkTime(int time) {
        if (time < 8 && time >= 1) {
            return true;
        } else {
            return false;
        }
        
    }
}
